name: plugin_channel
description: 负责 IDE 和插件的数据交互.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.2 <4.0.0"

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
dependencies:
  crypto: ^3.0.2
  path: ^1.8.0
